const Footer = () => {
    return (
        <footer className="footer">
            <p className="footer-copy">
                © {new Date().getFullYear()} Abitha.
            </p>
        </footer>
    );
};

export default Footer;
